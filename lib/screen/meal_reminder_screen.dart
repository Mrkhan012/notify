import 'package:flutter/material.dart';

class MealReminderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Meal Reminder')),
      body: Center(
        child: Text('This is the Meal Reminder Screen'),
      ),
    );
  }
}
