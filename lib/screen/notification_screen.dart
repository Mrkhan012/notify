import 'package:flutter/material.dart';

class StandardNotificationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Standard Notification')),
      body: Center(
        child: Text('This is the Standard Notification Screen'),
      ),
    );
  }
}
